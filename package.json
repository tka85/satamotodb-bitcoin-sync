{
  "name": "satamotodb-bitcoin-sync",
  "version": "2.0.0",
  "scripts": {
    "build": "rm -rf dist/* && npm run lint && tsc",
    "lint": "tslint --project tsconfig.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@types/big.js": "4.0.5",
    "@types/node": "13.11.1",
    "@types/pg": "7.14.3",
    "big.js": "5.2.2",
    "debug": "4.1.1",
    "json-rpc2": "2.0.0",
    "pg": "8.0.2",
    "tslint": "6.1.1",
    "typescript": "3.8.3"
  },
  "devDependencies": {},
  "description": "Application that exports Bitcoin's blockchain data (blocks, txs, inputs, outputs, addresses etc.) and stores them into a Postgres database. Used by the satamotodb project.",
  "author": "https://github.com/tka85",
  "license": "MIT",
  "engines": {
    "node": ">=10.13.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/tka85/satamotodb-bitcoin-sync.git"
  }
}